name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: services/control-plane-go/go.mod

      - name: Run verify
        run: npm run verify

      - name: Install Chromium for browser e2e
        run: npx --yes playwright install chromium

      - name: Run browser e2e release gate
        run: npm run verify:browser

      - name: Run review gate
        run: npm run gate:review
