const TEST_ENTRIES = Object.freeze([
  Object.freeze({
    file: 'tests/health/registry-coverage.test.mjs',
    runner: 'node',
    lane: 'health',
    ownerSurface: 'workflow',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['scripts/test-classification.mjs', 'AGENTS.md']),
    verifySuites: Object.freeze(['current', 'health']),
  }),
  Object.freeze({
    file: 'tests/health/repo-bloat.test.mjs',
    runner: 'node',
    lane: 'health',
    ownerSurface: 'repo-hygiene',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['scripts/repo-bloat-audit.mjs', 'AGENTS.md', 'TASTE.md', 'docs/status.md', 'docs/decisions.md']),
    verifySuites: Object.freeze(['current', 'health']),
  }),
  Object.freeze({
    file: 'tests/health/workflow-entrypoint.test.mjs',
    runner: 'node',
    lane: 'health',
    ownerSurface: 'workflow',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['.github/workflows/ci.yml', '.github/workflows/cloud-rollout.yml', '.github/workflows/release-image.yml', 'package.json', 'scripts/verify.mjs', 'scripts/workflow-gate.mjs']),
    verifySuites: Object.freeze(['current', 'health']),
  }),
  Object.freeze({
    file: 'tests/health/deploy-container-readiness.test.mjs',
    runner: 'node',
    lane: 'health',
    ownerSurface: 'deploy',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['Dockerfile', 'Dockerfile.cloud', '.dockerignore', '.dockerignore.cloud', 'services/control-plane-go/cmd/opl-webui-control-plane/main.go']),
    verifySuites: Object.freeze(['current', 'health']),
  }),
  Object.freeze({ file: 'tests/health/governance-hardening.test.mjs', runner: 'node', lane: 'health', ownerSurface: 'repo-governance', lifecycleRole: 'current-owner', contracts: Object.freeze(['README.md', 'AGENTS.md', 'TASTE.md', 'docs/status.md', 'docs/decisions.md', 'docs/docs_portfolio_consolidation.md', 'docs/active/README.md', 'docs/history/tombstones/README.md', 'contracts/web-product-profile.json']), verifySuites: Object.freeze(['current', 'health']) }),
  Object.freeze({ file: 'tests/health/stale-retirement-guard.test.mjs', runner: 'node', lane: 'health', ownerSurface: 'repo-governance', lifecycleRole: 'current-owner', contracts: Object.freeze(['README.md', 'AGENTS.md', 'TASTE.md', 'docs/invariants.md', 'docs/status.md']), verifySuites: Object.freeze(['current', 'health']) }),
  Object.freeze({
    file: 'tests/contract/fixed-truth-lifecycle.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'fixed-truth-lifecycle',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['README.md', 'AGENTS.md', 'TASTE.md', 'docs/status.md', 'docs/decisions.md', 'docs/active/README.md', 'docs/history/process/closeouts.md', 'docs/history/tombstones/README.md', 'deploy/web-cloud/RUNBOOK.md', 'contracts/web-product-profile.json', 'contracts/web-api.openapi.json', 'contracts/web-release-profile.json']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/contract/web-cloud-deploy-shape.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'deploy',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['deploy/web-cloud/opl-webui.k8s.json', 'deploy/web-cloud/RUNBOOK.md']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/contract/cloud-rollout-helper.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'deploy',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['scripts/cloud-rollout.mjs', 'deploy/web-cloud/RUNBOOK.md']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/contract/go-control-plane-http.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'control-plane-go',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['services/control-plane-go/cmd/opl-webui-control-plane/main.go', 'services/control-plane-go/internal/webapp/handlers.go', 'services/control-plane-go/internal/oplbridge/route.go']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/contract/one-person-lab-chat-upstream.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'control-plane-go',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze([
      'services/control-plane-go/cmd/opl-webui-control-plane/main.go',
      'services/control-plane-go/internal/webapp/handlers.go',
      'services/control-plane-go/internal/webapp/store_guardrails.go',
    ]),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/contract/opl-readonly-bridge.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'opl-bridge',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['services/control-plane-go/internal/oplbridge/snapshot.go', 'services/control-plane-go/cmd/opl-webui-control-plane/main.go']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/contract/one-person-lab-web-data.test.mjs',
    runner: 'node',
    lane: 'contract',
    ownerSurface: 'apps-web',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['apps/web/src/onePersonLabWeb.mjs', 'services/control-plane-go/cmd/opl-webui-control-plane/main.go', 'contracts/web-product-profile.json', 'contracts/web-page-state-matrix.json', 'contracts/web-api.openapi.json', 'contracts/web-runtime-bridge.json', 'contracts/web-release-profile.json']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'tests/smoke/foundation.test.mjs',
    runner: 'node',
    lane: 'smoke',
    ownerSurface: 'foundation',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['AGENTS.md', 'package.json', 'scripts/test-classification.mjs']),
    verifySuites: Object.freeze(['current', 'smoke']),
  }),
  Object.freeze({
    file: 'tests/smoke/web-shell.test.mjs',
    runner: 'node',
    lane: 'smoke',
    ownerSurface: 'apps-web',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['apps/web/index.html', 'apps/web/styles.css', 'apps/web/src/onePersonLabWeb.mjs']),
    verifySuites: Object.freeze(['current', 'smoke']),
  }),
  Object.freeze({ file: 'services/control-plane-go/cmd/opl-webui-control-plane/main_test.go', runner: 'go', cwd: 'services/control-plane-go', goPackage: './cmd/opl-webui-control-plane', lane: 'contract', ownerSurface: 'control-plane-go', lifecycleRole: 'current-owner', contracts: Object.freeze(['services/control-plane-go/cmd/opl-webui-control-plane/main.go']), verifySuites: Object.freeze(['current', 'contract']) }),
  Object.freeze({ file: 'services/control-plane-go/internal/controlplane/task_test.go', runner: 'go', cwd: 'services/control-plane-go', goPackage: './internal/controlplane', lane: 'contract', ownerSurface: 'control-plane-go', lifecycleRole: 'current-owner', contracts: Object.freeze(['services/control-plane-go/internal/controlplane/task.go']), verifySuites: Object.freeze(['current', 'contract']) }),
  Object.freeze({
    file: 'services/control-plane-go/internal/controlplane/postgres_store_test.go',
    runner: 'go',
    cwd: 'services/control-plane-go',
    goPackage: './internal/controlplane',
    lane: 'contract',
    ownerSurface: 'control-plane-go',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze([
      'services/control-plane-go/internal/controlplane/postgres_schema.go',
      'services/control-plane-go/internal/controlplane/postgres_store.go',
    ]),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'services/control-plane-go/internal/controlplane/postgres_test_helpers_test.go',
    runner: 'go',
    cwd: 'services/control-plane-go',
    goPackage: './internal/controlplane',
    lane: 'contract',
    ownerSurface: 'control-plane-go',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze([
      'services/control-plane-go/internal/controlplane/postgres_store.go',
    ]),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'services/control-plane-go/internal/controlplane/postgres_usage_ledger_test.go',
    runner: 'go',
    cwd: 'services/control-plane-go',
    goPackage: './internal/controlplane',
    lane: 'contract',
    ownerSurface: 'control-plane-go',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['services/control-plane-go/internal/controlplane/postgres_store.go']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'services/control-plane-go/internal/controlplane/store_test.go',
    runner: 'go',
    cwd: 'services/control-plane-go',
    goPackage: './internal/controlplane',
    lane: 'contract',
    ownerSurface: 'control-plane-go',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze([
      'services/control-plane-go/internal/controlplane/store.go',
    ]),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'services/control-plane-go/internal/oplbridge/snapshot_test.go',
    runner: 'go',
    cwd: 'services/control-plane-go',
    goPackage: './internal/oplbridge',
    lane: 'contract',
    ownerSurface: 'opl-bridge',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze([
      'services/control-plane-go/internal/oplbridge/snapshot.go',
      'services/control-plane-go/internal/oplbridge/route.go',
    ]),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
  Object.freeze({
    file: 'services/control-plane-go/internal/runtimegate/gate_test.go',
    runner: 'go',
    cwd: 'services/control-plane-go',
    goPackage: './internal/runtimegate',
    lane: 'contract',
    ownerSurface: 'production-runtime',
    lifecycleRole: 'current-owner',
    contracts: Object.freeze(['services/control-plane-go/internal/runtimegate/gate.go']),
    verifySuites: Object.freeze(['current', 'contract']),
  }),
]);

function lane(name, description) {
  return Object.freeze({
    description,
    tests: Object.freeze(TEST_ENTRIES.filter((entry) => entry.lane === name)),
  });
}

export const TEST_LANE_REGISTRY = Object.freeze({
  health: lane('health', 'Repository verification gate health checks.'),
  contract: lane('contract', 'Cross-package and lifecycle contracts.'),
  smoke: lane('smoke', 'Minimal foundation smoke checks.'),
  regression: Object.freeze({
    description: 'Explicit regression reproductions.',
    tests: Object.freeze([]),
  }),
});

export const VERIFY_SUITES = Object.freeze({
  current: Object.freeze(['health', 'contract', 'smoke']),
});
