# Spec Delta

- owner: mvp-task-artifact-loop
- state: active

## ADDED

- `apps/api` 提供本地可调用 demo task-artifact loop。
- `packages/core` 提供 task/artifact projection builder。
- smoke eval 验证 demo task -> mock OPL -> artifact。

## CANNOT-CLAIM

- 不能声明生产 SaaS、多租户鉴权、持久化、真实 OPL execution 或 WebUI 已完成。
