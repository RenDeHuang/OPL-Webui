# Spec Delta

- owner: web-demo-workspace-shell
- state: active

## ADDED

- `apps/web/index.html` 作为第一版中文 WebUI shell。
- `apps/web/styles.css` 作为局部样式。
- `apps/web/src/demoData.mjs` 从 API demo scenario 生成页面数据。

## CANNOT-CLAIM

- 不能声明已完成完整 Genspark 复刻、真实登录、多租户隔离或真实 OPL 执行。
