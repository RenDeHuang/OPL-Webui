# Design

- owner: web-demo-workspace-shell
- state: active

## Surface

首屏是 AI workspace，不是后台 dashboard：顶部轻导航，中间大输入框，下面是轻量任务/交付物面板。

## Data

Web demo 数据来自 `runDemoTaskArtifactScenario()` 的快照，避免页面自造业务 truth。
