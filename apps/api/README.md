# OPL-WebUI API

Owner: `OPL-WebUI`
Purpose: `saas_api_control_plane`
State: `planned`
Machine boundary: This file is human-readable only. API behavior must be verified by contracts and tests.

This directory will hold the SaaS API that calls `packages/opl-adapter`; browser code must never call OPL CLI directly.
