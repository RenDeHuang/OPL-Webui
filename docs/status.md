# One Person Lab Web Status

- owner: product-engineering owner
- purpose: current status, can/cannot-claim boundary, and next development priorities.
- state: active
- machine boundary: human-readable status; contracts, source, tests, scripts, API behavior, deploy artifacts, and Sentrux rules are authoritative.

## Current State

One Person Lab Web product truth is fixed to three layers. The top-level product is a One Person Lab knowledge delivery Web platform for personal researchers and research groups or organizations across research, paper, grant, review, file, PPT, and book knowledge delivery scenarios. Public Growth Layer is done v1 at repo/browser evidence level for unauthenticated users to understand the product, use cases, task/skill entries, expected refs/outputs, trust boundary, and login CTA; it does not own runtime execution, artifact body, or billing truth. Account-based User Product Layer is done v1 at repo/browser evidence level: open Web, log in, bind an API Key or use MedOPL account/resource-state capability when ready, choose a research/fund/PPT/book task entry, then see result or MedOPL gate, progress refs, deliverable refs, blocker/next step, task history/deliverable continuation center v0, and MedOPL/OPL continuation. Minimal Admin/Ops Layer is partial: registration policy and user status v0 is done through operator-only API for sanitized status/quota/audit reads, fail-closed non-open registration, disabled-user denial, and audited disable/enable. Dogfood/release evidence summary is not_started, and hidden `/_ops` UI is optional/reserved. It is not payment, team/RBAC, HA, runtime sync, support impersonation, or full SaaS.

Current product work uses three-layer product modules plus explicit gap slices. Product work moves one slice at a time. The account-based user product layer is done v1 at repo/browser evidence level; the public growth layer is done v1 at repo/browser evidence level; minimal admin/ops remains partial after `registration_policy_user_status_v0`.

`docs/active/README.md` now carries the lightweight current gap baton for worktree lanes and next-agent handoff. It supports the fixed truth set; it does not replace this status file or any machine contract.

The current gap set is machine-owned by `contracts/web-product-profile.json#/gapMap`: growth layer is `done_v1`, user product layer is `done_v1_repo_browser`, admin ops layer is `partial`, and production rollout is `folded_success_run_28312150030`. Production deploy is release evidence only and must not substitute for product/eval work.

## Machine Truth

- Product boundary: `contracts/web-product-profile.json`
- GUI product grammar: `contracts/web-gui-product-contract.json`
- Page state: `contracts/web-page-state-matrix.json`
- HTTP API: `contracts/web-api.openapi.json`
- Runtime gate and refs-only projection: `contracts/web-runtime-bridge.json`
- Release readiness: `contracts/web-release-profile.json`
- Gap phase queues and runtime cleanup policy: `contracts/web-gap-phase-registry.json`
- AI development discipline: `contracts/web-development-profile.json`
- Long-lived surface ownership: `contracts/web-surface-inventory.json`
- Historical process evidence: `docs/history/process/closeouts.md`
- Active gap baton: `docs/active/README.md`
- Retired surface tombstones: `docs/history/tombstones/README.md`
- Production runbook evidence: `deploy/web-cloud/RUNBOOK.md`

Markdown docs explain those contracts. If docs and contracts disagree, update the contract or retire the stale prose in the same diff.

## Development System Status

- `changes/active` is retired as the default development system.
- Fixed truth entries are `README.md`, `AGENTS.md`, `TASTE.md`, `docs/project.md`, `docs/status.md`, `docs/decisions.md`, `docs/architecture.md`, `docs/invariants.md`, `docs/docs_portfolio_consolidation.md`, and `contracts/*.json`.
- `docs/status.md` keeps the current gap and handoff boundary.
- `docs/active/README.md` keeps the worktree lane baton for current/vision-driven development.
- `docs/history/process/closeouts.md` is historical provenance only; it must not become current truth.
- `docs/history/tombstones/README.md` records retired surfaces and no-resurrection rules.
- Test workflow now uses fixed main lanes plus dynamic targeted lane selection. `current = smoke + contract + health + go`; `browser`, `deploy`, `regression`, `real-medopl`, and `full` are explicit lanes for changed surfaces and release-risk work. `real-medopl` is intentionally not part of `current` or `full` because it depends on a real MedOPL repo/process.
- `scripts/test-classification.mjs` is the machine registry for lane membership, cost, lifecycle role, risk triggers, verify suites, and regression retirement metadata.
- Test proof taxonomy is now mandatory in the registry: every test declares testKind, proofLevel, claimScope, proves, and doesNotProve. Static/unit/http/browser evidence cannot be used to claim production readiness unless production evidence is freshly folded back.
- `contracts/web-surface-inventory.json` owns long-lived surface registration for scripts, contracts, tests, Go tests, recurring docs, workflows/deploy, and selected source owner surfaces. Ordinary implementation files are intentionally excluded so inventory does not become a second source tree.
- `npm run repo:bloat` now reads the surface inventory. Owned growth is report-only; orphan growth without owner/consumer/contract or machine boundary is a hard failure.
- `scripts/lane-advisory.mjs` maps changed files to targeted lanes for operator visibility; lane-check/gate evidence decides whether the required targeted lanes were actually run for the current diff.
- `contracts/web-gap-phase-registry.json` turns broad goals into phase queues with owner surfaces, current-step objective, acceptance gates, owner receipt policy, next-step openers, required evals, evidence sources, typed blockers, and cannot-claim boundaries. `npm run gap:phase` reports `done` / `partial` / `blocked` / `not_started` status, multi-dimensional `evalResults`, `readyToAdvance`, and `readyToAdvanceBlockedBy`; a phase may advance only when `currentStatus=done` and all eval results pass. Repo-local tests cannot infer production evidence or owner receipt.
- Gap phase run artifacts are not durable truth. `.runtime/phase-runs` is git-ignored, capped by TTL/count/size policy, and cleaned with `node scripts/gap-phase-runner.mjs cleanup`; only sanitized evidence summaries may fold back into contracts or status.
- `contracts/web-development-profile.json` defines the AI development order, anti-bloat policy, task tiers, and completion boundaries; `npm run gate:ai` enforces claim freshness and workflow hygiene, and `npm run gate:review` runs it before lane evidence and current verify.
- Release evidence foldback is exposed as `npm run release:evidence -- --run-id <github-run-id>` so production rollout/browser/dogfood/availability evidence can update the release profile and status as a first-class workflow step.
- `regression` can be empty. Active `regression-guard` tests must carry retirement metadata, and when the condition is met the test, registry entry, and any needed tombstone cleanup happen in the same change.

## Can Claim

- One Person Lab Web is a One Person Lab knowledge delivery Web platform at `opl.medopl.cn`.
- Three product layers are the fixed product truth: Public Growth Layer, Account-based User Product Layer, and Minimal Admin/Ops Layer.
- The account-based user product layer is done v1 at repo/browser evidence level, including task history/deliverable continuation center v0.
- The public growth layer is done v1 at repo/browser evidence level: anonymous users see the knowledge delivery positioning, task entries, output/ref expectations, audience, trust boundary, and login/register start path; clicking a public CTA or task entry opens login/register and restores the selected task entry after auth.
- Research staff, master's students, PhD students, principal investigators, and research teams enter through `@科研`, `@论文`, `@基金`, `@综述`, `@文件`, `@PPT`, and `@书`.
- Ordinary chat is a fallback entry, not the primary product positioning.
- The Web UI calls only the same-origin Go control plane HTTP API.
- Users bind their own API Key while provider `base_url` remains fixed at `https://gflabtoken.cn/v1`.
- Ordinary chat upstream calls use the fixed `gpt-5.5` Responses API profile with `service_tier=fast`, `reasoning.effort=xhigh`, and a configurable response-header timeout budget. The default production budget is `60s` via `OPL_CHAT_UPSTREAM_TIMEOUT_SECONDS`; timeout failures remain fail-closed with sanitized `chat.upstream_failed` audit evidence.
- Desktop App packaging/updater, OPL Framework runtime truth, domain-agent judgment authority, billing source of truth, storage truth, node pool lifecycle, API gateway truth, OPL execution truth, and artifact/body authority are not owned by this repo.
- Runtime-requiring markers such as `@论文`, `@基金`, `@综述`, `@文件`, `@PPT`, and `@书` first call the Go control plane MedOPL bridge `POST /api/opl/runtime-gate`; the browser calls `POST /api/opl/runs` only after MedOPL returns account/resource-state ready. Missing API key, missing package/plan/credit, compute, storage, workspace/runtime/storage binding, billing blocked, release required, and audit required are MedOPL account/resource blockers, not fake ready states. Missing `MEDOPL_API_BASE_URL` is an OPL-Webui operator deployment blocker (`medopl_endpoint_required`), not a user access-policy failure.
- Runtime bridge boundary now has a local Go-side MedOPL API bridge for gate/run refs-only projection and billing ledger refs projection. The explicit `real-medopl` lane (`npm run verify:real-medopl`) starts a real local MedOPL Go backend process from `/home/dev/projects/platform-v22/services/medopl-go-backend` and verifies WebUI -> runtime-required task -> MedOPL runtime gate blocker -> MedOPL local credit/provider/open runtime+storage owner action -> return_to_opl_task -> WebUI `/api/opl/runs` with MedOPL launch/file refs -> refs-only artifact projection -> billing ledger refs projection -> MedOPL release/storage owner actions -> WebUI readonly release/storage projection. runtime/storage/package/billing/resource release remain MedOPL-owned capabilities; OPL Framework/domain agents remain execution semantics, quality verdict, and artifact authority. Current Web evidence covers local real MedOPL process behavior, local contract behavior, MedOPL account/resource-state admission, typed blockers, readonly refs/progress/deliverables projection, deeplink, ledger refs, release/storage projection, and fail-closed gate, not production/sandbox MedOPL runtime execution, storage truth, real cloud node lifecycle, external payment settlement, production release execution, or artifact body authority. Local real MedOPL proof is an evidence lane, not product truth or product access policy.
- Local no-secret readiness is machine-owned by `contracts/web-release-profile.json` as HTTP contract, static shell evidence, and browser e2e evidence. The explicit browser lane ran real Chromium/CDP automation through `npm run verify:browser`.
- Controlled launch readiness is a separate release claim over `contracts/web-product-profile.json#/webBusinessCapabilityV1`: hard gates are image existence, dry-run, apply, canary/smoke, availability probe, account/session/BYOK/audit/quota/Postgres contracts, service-unavailable fail-closed behavior, and the account-based Web App business path contract. Real upstream structured research success is supporting evidence, not the only hard gate; an upstream `service_unavailable` attempt cannot by itself invalidate the controlled launch claim if fail-closed behavior and launch gates are intact. Latest main production evidence is folded back from GitHub Actions run `28312150030` for commit `1c71ddc05deceda3d2f2cebde06d2fefb2a0fc31`, image `uswccr.ccs.tencentyun.com/webopl/opl-webui:1c71ddc`; historical run `28142197152` remains historical-only release evidence and does not auto-apply to later main commits.
- Latest-main production authenticated dogfood HTTP evidence executed successfully in GitHub Actions run `28312150030` for commit `1c71ddc05deceda3d2f2cebde06d2fefb2a0fc31`, image `uswccr.ccs.tencentyun.com/webopl/opl-webui:1c71ddc`, after dry-run, image preflight, production apply, availability probe, dogfood, and production browser e2e success. Production real ordinary chat completion, MedOPL readonly projection checks, and `chat.completed` audit evidence are folded back as sanitized latest-main release evidence. This is not MedOPL runtime execution evidence. Historical run `28142197152` remains provenance only.
- Real local Chromium browser e2e executed successfully through `npm run verify:browser`, covering register, login, API Key binding, ordinary chat with mock upstream, `@论文`/`@基金` runtime gates, sanitized audit evidence, and user-like CDP input.
- Browser e2e is now a CI release gate before image release: `.github/workflows/ci.yml` installs Chromium and runs `npm run verify:browser`; `Release Image` still depends on successful CI before pushing the cloud image.
- Account-based Web App UX is implemented in the browser shell and page-state contract: the first-screen task templates cover `research_direction`, `paper_question`, `grant_plan`, `review_map`, `materials_refs`, `presentation_foundry`, and `book_foundry`; `@科研` ordinary chat renders a structured result view model with `research_plan`, `evidence_refs`, and `next_steps`; runtime-required markers render a runtime task card with MedOPL deep link and forbidden Web execution metadata. This is UI view-model evidence only, not artifact body, storage, or runtime execution.
- Latest-main production browser e2e executed successfully in GitHub Actions run `28312150030` through the secret-gated real Chromium/CDP lane `node tests/browser/research-main-path-runner.mjs --production` and `Cloud Rollout` input `production_browser_e2e=true`. It ran after production apply and used the production dogfood account to verify real browser login, API Key binding, task selection, structured result/gate, refs/deeplink continuity, and sanitized audit evidence against `https://opl.medopl.cn`. The real upstream structured research success is supporting evidence for controlled launch, not a standalone upstream availability guarantee. A later or future `service_unavailable` attempt must be recorded as fail-closed evidence and cannot claim upstream provider availability.
- GitHub Actions run `27877811961` failed only at `Production Browser E2E` after dry-run, apply, availability probe, and authenticated dogfood passed. The folded failure kind was `response_header_timeout` from `gflabtoken.cn` on model `gpt-5.5`, so current remediation is timeout-budget hardening for real production browser chat, not API key/model rotation and not MedOPL runtime expansion.
- Production dogfood readonly projection was confirmed for latest-main run `28312150030` with sanitized `OPL_PRODUCTION_DOGFOOD_MEDOPL_READONLY=1` foldback. It can claim production MedOPL readonly projection dogfood only; it does not prove MedOPL runtime execution, payment, storage mutation, node lifecycle, or production MedOPL API integration.
- Latest-main production availability probe executed successfully in GitHub Actions run `28312150030` after production apply on image `uswccr.ccs.tencentyun.com/webopl/opl-webui:1c71ddc`. The no-secret public HTTP probe checks `/healthz`, `/readyz`, `/metricsz`, and `/` repeatedly without kubeconfig, image, DB, dogfood secrets, MedOPL token, or TCR credentials.
- GitHub Actions run `27878485498` exposed a release-order issue: `Production Apply` targeted image `uswccr.ccs.tencentyun.com/webopl/opl-webui:80689b1`, but the TCR manifest did not exist, so Kubernetes reached `ImagePullBackOff` while the previous Ready pod stayed serving. Cloud Rollout now has a no-kubeconfig `Production Image Preflight` stage and `node scripts/cloud-rollout.mjs --image-preflight`; missing image manifests fail closed as `image_missing_rollout_order_issue` with `imagePullOccurred=false` before `kubectl set image`.
- Production observability baseline v1 is folded back to latest-main run `28312150030`: `/metricsz` exposes `observabilitySchemaVersion=1`, `releaseProbeContract=production_observability_baseline_v1`, and public probe endpoints; `node scripts/cloud-rollout.mjs --availability-probe` emits per-endpoint sample/success/failure/max-duration summaries without raw logs or secrets. A no-secret scheduled availability canary first succeeded in GitHub Actions run `27874732529` through `.github/workflows/production-canary.yml`. Production operations readiness is now split into P0, P1, and P2 gates in `contracts/web-release-profile.json`: P0 launch operations contracts are present for rollback path, alerting boundary, DB backup/restore strategy, security ops baseline, incident runbook owner, and cost/quota guard; P1 commercial operations contracts are present for staging/production concurrency evidence, upstream backpressure, migration/schema compatibility, and observability dashboard entry; P2 SLA/HA operations contracts are present for HA topology evidence, SLO/error budget, and automatic rollback admission. Alert route owner receipt is accepted for owner `huangrende` with P0 5 minutes, P1 30 minutes, and P2 business-hours response, but channel evidence remains pending. Staging/load policy is accepted as staging 10 users baseline with production smoke only and no real `gflabtoken.cn` quota consumption unless separately authorized. `productionOperationsCloseout` defines the P0/P1 single-node launch operations receipt for rollback, alerting, DB restore, monitoring/dashboard, soak, load, DB pool, upstream backpressure, and migration compatibility through `npm run release:evidence -- --ops-closeout-json <sanitized-json>`. `productionLaunchCloseout` defines the final release decision receipt contract for broader go/no-go evidence. The external production evidence still pending after this RC closeout: P0/P1 operations closeout, final release decision receipt, production rollback record, alert channel, DB restore drill, dashboard URL, production load run, long soak, multi-node HA run, SLO enforcement, or automatic rollback admission.
- Production HA is paused for the current single-node launch. HA owner decision is accepted as paused: no second TKE node, no `replicas=2`, no PDB/topology-spread rollout in this phase, and HA does not block the current single-node controlled launch. `deploy/web-cloud/opl-webui.k8s.json` intentionally applies `replicas=1` so a one-worker TKE cluster does not deadlock on `DoNotSchedule` topology rules. `contracts/web-release-profile.json` keeps the future HA evidence boundary: `replicas=2`, two Ready Pods, distinct nodes, `PDB minAvailable=1`, topology spread constraints, rolling update `maxUnavailable=0`, at least two healthy Ingress/CLB backends, canary/smoke, and production availability probe. Until cloud execution evidence is folded back, this repo cannot claim multi-node HA, CLB two-backend health, or zero-downtime rollout evidence.
- Manual production rollback evidence is now supported by `Cloud Rollout` input `rollback=true` and `node scripts/cloud-rollout.mjs --rollback`. It requires production environment approval, kubeconfig, image allowlist validation, rollout status, canary db, canary opl-cli, HTTPS smoke, and optional availability probe after rollback. This is manual rollback evidence, not automatic rollback or data migration rollback.
- Commercial account lifecycle projection is implemented as authenticated readonly personal commercial status projection, and owner decision accepts that this boundary does not block the current single-node controlled launch. `GET /api/account/commercial-status` returns personal account state, tenant role, lifecycle state, `teamReadiness=single_user_owner`, allowed next action `view_medopl_billing`, and forbidden team/invite/RBAC/payment/billing-source mutation flags. The current browser UI does not dump lifecycle, quota, audit, provider, auth, or API Key status into More; account/login/API Key live behind the bottom avatar popover and More is an empty/light overflow surface. This reuses the existing account/session and hidden tenant isolation surface; it does not add team management, RBAC, invite, pricing, payment, plan, subscription, storage, node pool, or runtime ownership. Billing/payment source of truth remains MedOPL. Full commercial platform depth remains blocked for future expansion until a real buyer/operator workflow exists. Any team invite/RBAC/pricing/subscription/payment expansion requires structured `expansionConditions`: real consumer, surface contract, registered tests, MedOPL billing authority preservation, and payment processor contract; `webRoutesMayMutateBilling=false` until those conditions are admitted.
- User-facing reliability UX is implemented for current Web states: the data layer converts auth-required, API-key-required, quota, upstream failure, service-unavailable, and network-unreachable results into sanitized view models; the browser shell renders a stable reliability status surface without raw upstream bodies, API keys, DB URLs, or private state.
- UI/UX product depth now has source-level Figma MCP evidence pinned in `contracts/web-gui-product-contract.json`: file `1MNO5l7PQYKZVNqQgw6DGS`, node `0:1`, required source files `src/app/App.tsx`, `src/styles/theme.css`, and `src/styles/index.css`. The UI governance rule is subject-first: the current subject is an AI-native research composer with project/session/result workflow, not a dashboard template, CRM template, settings center, runtime console, component inventory, or card grid first surface. Components follow surface ownership and behavior: account/model metadata use popovers, conversation history and mobile inspector use sheets, API Key unblock uses a dialog, destructive confirmation alone uses alert dialog, and cards carry starter chips, library items, or result artifacts rather than routine section wrappers. The current repo-local UI variant is `ai_native_research_home_v1`: the shell uses side navigation, a first-view center composer, starter chips, route-scoped Skill/工作流/Projects surfaces, conversation-history search, empty More overflow, bottom-avatar account popover, optional model selector without visible provider base URL, one structured `@科研` artifact card instead of duplicated raw upstream transcript, a stable desktop right inspector work panel, and lightweight mobile bottom sheets. The visual contract freezes OPL green as the primary accent, rejects default tech blue/purple primary palettes, requires hover/active/focus-visible/disabled/loading states, requires a `prefers-reduced-motion` path, and now carries visual identity, visual quality rubric, and owner receipt protocol as existing-contract rules rather than a new UI governance doc. It rejects Drive/storage ownership, runtime truth ownership, founder-plan upsell, unlimited compute claims, dashboard/CRM primary app, generic office/content/code/video skill surfaces, visible fixed base_url in the user shell, settings/status dumps in More, fake refs/test evidence copy, floating desktop inspector overlays, heavy mobile sheets, raw structured-result transcripts, and retired shell structures. Real local Chromium/CDP browser evidence captures desktop, tablet, mobile, and compact screenshots into git-ignored `.runtime/browser-visual/` artifacts and asserts no horizontal overflow, no static text overflow, inspector within viewport, desktop inspector stable panel, mobile inspector bottom sheet within height budget, artifact-first research result density, active inspector panel visibility, touch target pass, named controls pass, keyboard focus visibility, keyboard path pass, API Key modal focus trap pass, contrast pass, no hidden overlay input interception, and pass/fail visual quality rubric through `npm run verify:browser`. Completed repo-local UI phases and repo-local accessibility closeout are folded into the GUI contract and tests. Production run `28039468173` provides sanitized production browser evidence against `https://opl.medopl.cn`, and the human owner accepted `ui_ux_v1_production_accepted` for the current production v1 surface on `2026-06-23`. Long-term truth for this claim is limited to `contracts/web-gui-product-contract.json`, `contracts/web-page-state-matrix.json`, `contracts/web-gap-phase-registry.json`, `contracts/web-product-profile.json`, registered tests, `docs/status.md`, and `docs/active/README.md`; no new UI governance file is needed while those owners carry the rule. Temporary artifacts are `.runtime/browser-visual/*`, `.runtime/phase-runs/*`, production raw logs, raw screenshots, and CI raw output; they must not be committed, and only sanitized summaries may fold back. Complete UI/UX design system, assistive technology conformance, and production visual polish remain unclaimed.
- Figma Make `UI UX for Commercial Launch` is accepted as the Figma parity UI replacement target in `contracts/web-gui-product-contract.json#/figmaParityUiReplacementTarget`, not a generic reference and not a partial visual reuse source. It applies to Public Growth Layer and Account-based User Product Layer only: product introduction, task scenarios, output showcase, start path, authenticated workbench, task entry, task history, refs, blocker/next step, and MedOPL/OPL deeplink. It is not an Admin/Ops, `/_ops`, or operator controls target. The Figma parity UI replacement candidate is deployed to production controlled launch at commit `1c71ddc05deceda3d2f2cebde06d2fefb2a0fc31` with Cloud Rollout run `28312150030`; old UI surfaces are retired from the current production shell and must not be kept as a parallel shell. This rollout changes UI evidence only and does not expand release claims beyond controlled launch readiness. Current cannot-claim entries such as payment, runtime execution, storage truth, artifact body authority, team/RBAC, HA, and full SaaS are current-stage evidence boundaries, not permanent product vision denials. Future commercial/payment, MedOPL-owned runtime execution, storage/artifact, team/RBAC, or organization workspace capability may be introduced only through explicit consumer, contract, registered tests/eval, owner receipt, and folded evidence. Replacement copy follows canonical grammar: `Commercial Launch` becomes Controlled Knowledge Delivery / One Person Lab Web; `Pro`/`credit`/`recharge` become account capability, API key, quota projection, or external capability handoff; `runtime completed` becomes MedOPL continuation ready / refs available; `storage ready` becomes MedOPL storage status projection; `artifact body` becomes deliverable refs; and `payment status` becomes readonly commercial projection.
- Runtime bridge boundary is now owner-accepted as local MedOPL gate/run refs-only projection and has real local MedOPL process evidence: runtime admission is driven by MedOPL account/resource state, not selected/test/canary users or a hardcoded account allowlist. `MEDOPL_API_BASE_URL` is operator deployment config. Web does not install, repair, module exec, return artifact body, own domain verdict, mutate runtime, mutate storage, or mutate billing. MedOPL / OPL Framework remain execution and artifact authority; Web may submit task intent plus refs/context through the Go control plane and render typed blockers, progress refs, deliverable refs, and MedOPL next actions. Production/sandbox MedOPL runtime execution/API evidence, external payment settlement, production release execution, stop-billing, storage destroy, tenant-scoped run audit closeout, and artifact/body authority remain unclaimed; `webRoutesMayMutateRuntime=false` remains the current contract.
- Operations maturity now has partial evidence boundaries: `/metricsz`, availability probe, scheduled canary, manual rollback harness, rollback-record contract, P0/P1/P2 operations readiness contracts, P0/P1 operations closeout contract, and final release decision receipt contract are present. Existing evidence is baseline/release evidence plus owner decision boundaries only; it is not long soak, production load, rollback execution, alert channel, DB restore drill, dashboard URL, error-budget enforcement, automatic rollback, HA, final release approval, or production-ready SaaS evidence.
- The active gap steps now have repo-local phase acceptance boundaries: UI/UX `production_ui_quality_claim`, MedOPL readonly foldback, runtime gate/run bridge admission, commercial consumer contract, operations evidence contracts, HA single-node pause, concurrency/load evidence, and OPL runtime sync admission. The runner can advance only when the current step is `done` and all multi-dimensional eval results pass; UI/UX v1, MedOPL readonly production dogfood, local MedOPL gate/run bridge refs-only boundary, staging-safe concurrency/load v1, and OPL build-time pinned update policy are accepted. Operations maturity is partial because external production evidence remains pending; commercial depth remains blocked and HA remains paused.
- Concurrency/load is accepted only as a staging-safe v1 baseline. Local contract and Go tests cover tenant-scoped account/session/API key/chat isolation, the Postgres-backed store path when `OPL_DATABASE_URL` is set, a 10-user concurrent account/API-key/chat/quota baseline, bounded Postgres pool configuration, and slow-upstream timeout fail-closed behavior. This does not prove production concurrent SaaS readiness, production DB pool capacity, production upstream backpressure, or rate limiting.
- OPL auto-update from GitHub is accepted only as a build-time pinned image-release policy. Current Web releases rebuild the cloud image with a pinned OPL CLI/framework context and roll out an immutable tag; the pod does not `git pull`, self-update, install packages, or sync unverified GitHub HEAD. Runtime continuous sync remains forbidden until owner policy, signed source/release verification, rollback plan, registered eval, and tenant-safe maintenance window are admitted.

## Cannot Claim

- 还不是完整 production-ready SaaS。
- 本阶段没有证明 multi-node HA；当前 availability probe 只证明 public HTTP endpoints 的重复可用性。
- 本阶段没有 dashboard、alerting、error budget enforcement、production rollback execution 或 automatic rollback；只存在 rollback record contract。
- 本阶段没有新增 team invite、RBAC、pricing、subscription、payment mutation、billing source of truth、workspace-visible UI 或 commercial admin console；当前只能 claim authenticated readonly personal commercial status projection。
- Admin/Ops v0 只实现最小 registration policy / user status operator-only API、disabled-user denial、sanitized read 和 audit；不能 claim full SaaS、payment、team/RBAC、support impersonation、HA 或 runtime sync。
- 本阶段没有新增 billing truth、storage truth/mutation owner、OPL worker、object storage mutation、artifact body endpoint、payment lifecycle、production release execution、production stop-billing/storage-destroy 接入或 production/sandbox MedOPL runtime execution/API evidence。已有的是本地 Go-side MedOPL runtime gate/run bridge、refs-only projection contract，以及真实本地 MedOPL Go backend 进程的跨仓链路证据。
- 不能执行 OPL install、repair、module exec、family-runtime mutation、engine install/update/remove。
- 不能返回 artifact body、memory body、domain verdict、private state path、mutation result 或 raw provider secret。
- 不能 claim complete UI/UX design system、assistive technology conformance 或 production visual polish；当前只接受 `ui_ux_v1_production_accepted` 作为 current production v1 product surface claim。
- 不能 claim production concurrent SaaS readiness、production load-tested multi-user chat、production database pool sizing readiness、production slow upstream backpressure readiness、runtime continuous GitHub sync 或 one-person-lab-app background updater parity。

## Business Flow Gap Map

Machine-readable source: `contracts/web-product-profile.json#/businessFlowGapMap`.

Layer gap source: `contracts/web-product-profile.json#/gapMap`.

- `growth_layer`: done_v1. Public homepage/use cases/task catalog/output refs/audience/trust boundary/login CTA and anonymous login-return task path are implemented with contract, smoke, and browser evidence. Cannot claim authenticated task success, runtime execution, artifact body authority, full SaaS, or payment/team/RBAC/HA.
- `user_product_layer`: done_v1_repo_browser. Current account-based Web App path has repo/browser contract evidence, including task history/deliverable continuation center v0. Remaining gap is production/sandbox MedOPL account-resource-state evidence outside OPL-Webui.
- `admin_ops_layer`: partial. `registration_policy_user_status_v0` covers operator-token-protected registration policy, sanitized user/quota/audit read, disabled-user denial, and audited enable/disable at local HTTP evidence level. Dogfood/release evidence summary is not_started and hidden `/_ops` UI is optional/reserved. Cannot claim payment, pricing, subscription, invoice, refund, team/RBAC, support impersonation, HA, runtime sync, or full SaaS.
- `production_rollout`: folded_success_run_28312150030. Latest-main production evidence is folded back for controlled launch/readiness boundaries, but it cannot claim full SaaS, HA, payment/team/RBAC, runtime sync, or production MedOPL runtime execution.

- `login_register`: done. Web owns account register/login/session; proof is `tests/contract/go-control-plane-http.test.mjs`. Cannot claim email verification or password reset.
- `api_key_binding`: done. Web owns BYOK binding with fixed provider base URL; proof is `tests/contract/go-control-plane-http.test.mjs`. Cannot claim user-editable base URL or raw key exposure.
- `ordinary_chat`: done. Web owns ordinary chat fallback through fixed upstream after auth/key/quota; proof is `tests/contract/one-person-lab-chat-upstream.test.mjs`. Cannot claim upstream availability or domain artifact authority.
- `runtime_task_entries`: done. Web owns `@科研`, `@论文`, `@基金`, `@综述`, `@文件`, `@PPT`, and `@书` entry state; proof is `tests/contract/web-runtime-state.test.mjs`. Cannot claim Web-owned execution semantics.
- `runtime_gate`: partial / evidence gap. Local bridge, typed blockers, MedOPL account/resource-state admission, and real local MedOPL process blocker/ready evidence exist; production/sandbox MedOPL account/resource-state evidence is not folded back. Missing `MEDOPL_API_BASE_URL` is an operator deployment blocker, not user admission policy.
- `purchase_package`, `compute_resource`, `storage_space`: partial / evidence gap. MedOPL owns these; Web displays blockers/deeplinks and can consume local MedOPL open-runtime/storage refs, but production package/payment/compute/storage lifecycle evidence is not folded back.
- `runtime_run`: partial / evidence gap. Local `POST /api/opl/runs` refs-only bridge exists and has real local MedOPL launch/file refs evidence; production runtime execution and artifact/domain authority remain unclaimed.
- `progress_refs_deliverables`: partial / evidence gap. Web renders refs/progress/deliverables projections only; local MedOPL artifact refs are projected, while artifact body and storage mutation remain external.
- `billing_ledger_release`: partial / integration gap. Web can project billing summary/ledger refs from local MedOPL and readonly production dogfood; billing truth, production release execution, stop-billing, and storage destroy remain MedOPL gaps.
- `artifact_authority_domain_truth`: blocked / cannot-claim. OPL Framework/domain agents own verdict and artifact authority.

## Next Priorities

1. Continue product development from the current three-layer module truth: keep framework/governance frozen unless a real product gap requires a source, contract, test, or deploy change. Latest-main production evidence is folded back only for commit `1c71ddc05deceda3d2f2cebde06d2fefb2a0fc31`; future main commits require fresh foldback, and historical production run `28142197152` remains historical-only evidence.
2. Review the deployed Figma parity UI replacement candidate on the live URL, then open only targeted UI hardening slices for concrete mismatches. Complete design system, assistive technology conformance, and production visual polish remain separate future claims.
3. Keep future MedOPL readonly production evidence folded back through sanitized `release:evidence`; do not store raw logs or secret values in git.
4. Add team/commercial account lifecycle expansion only after there is a real consumer for invite/RBAC/pricing/subscription/payment state, a contract, and registered tests that preserve MedOPL billing authority.
5. Add dashboard, alerting, error budget, production rollback record evidence, or runtime execution only after the corresponding owner, consumer, contract, eval/test, and acceptance evidence exist.
