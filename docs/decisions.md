# One Person Lab Web Decisions

- owner: product-engineering owner
- purpose: current durable decisions and superseded readings.
- state: active_truth_with_history_notes
- machine boundary: human-readable decision record; contracts, source, tests, scripts, API behavior, and deploy artifacts are authoritative.

## Current Decisions

| Decision | Current reading | Machine boundary |
| --- | --- | --- |
| Web repo is the SaaS Web product peer of the App repo | `one-person-lab-web` is the multi-tenant SaaS Web edition of One Person Lab. It owns the browser product entry for research staff, master's students, PhD students, principal investigators, and research teams; `one-person-lab-app` owns the desktop product entry. | `contracts/web-product-profile.json`, `docs/project.md`, `docs/architecture.md` |
| Development uses fixed truth entries | Current truth lives in README, core docs, `docs/status.md`, `docs/decisions.md`, contracts, source, tests, and scripts. Per-change `changes/active` packages are retired as the default workflow. | `AGENTS.md`, `TASTE.md`, `docs/docs_portfolio_consolidation.md`, `tests/contract/fixed-truth-lifecycle.test.mjs` |
| Active docs are lightweight batons | `docs/active/README.md` is allowed for current gap, worktree lane, next-agent context, and foldback target. It is not a process package and not machine truth. | `docs/active/README.md`, `docs/docs_portfolio_consolidation.md`, `tests/contract/fixed-truth-lifecycle.test.mjs` |
| Tombstones prevent surface resurrection | Retired routes, modules, docs, names, and workflow surfaces belong under `docs/history/tombstones/` only when a no-resurrection record is useful. | `docs/history/tombstones/README.md`, `tests/health/stale-retirement-guard.test.mjs` |
| Repo bloat uses portfolio signals, not file-count blocking | Durable file count is reported as a soft portfolio signal. Hard failures stay on line-budget violations and concrete retired-surface or registry violations. | `scripts/repo-bloat-audit.mjs`, `tests/health/repo-bloat.test.mjs` |
| Primary entry is research capability first | Users enter through `@科研`, `@论文`, `@基金`, `@综述`, `@文件`, `@PPT`, and `@书`. Ordinary chat remains a fallback entry, not the primary product positioning. | `contracts/web-product-profile.json`, `contracts/web-page-state-matrix.json`, `contracts/web-runtime-bridge.json` |
| Product expansion resumes gap by gap | The heavy governance/change loop is retired. Future work should move through one product gap at a time with contract-first updates, registered tests, cleanup, and fresh evidence. | `docs/status.md`, test registry |
| Product gaps use acceptance contracts before deploy evidence | UI/UX depth, MedOPL readonly evidence, runtime execution boundary, commercial SaaS depth, operations maturity, HA/resilience, concurrency/load, and OPL auto-update from GitHub are tracked in `visionGaps` and the gap phase registry. HA is paused for single-node launch safety. Production deploy can fold back evidence, but it must not substitute for contract/eval/source/test work. | `contracts/web-product-profile.json`, `contracts/web-gap-phase-registry.json`, `contracts/web-gui-product-contract.json`, `contracts/web-release-profile.json`, `contracts/web-runtime-bridge.json` |
| Goal execution uses a lightweight gap phase registry | Broad goals advance through `web-gap-phase-registry` phase queues with owner surfaces, required evals, evidence sources, typed blockers, cannot-claim boundaries, and runtime artifact cleanup. The repo borrows the stage-led pattern without importing a full OPL Meta Agent runtime or keeping durable per-step process files. | `contracts/web-gap-phase-registry.json`, `scripts/gap-phase-runner.mjs`, `tests/health/gap-phase-system.test.mjs` |
| UI work starts from Figma MCP source context | Further UI/UX work must refresh Figma MCP source context first, then adapt only Web-owned patterns into the static shell. Figma patterns that imply Drive/storage ownership, runtime truth ownership, founder-plan upsell, unlimited compute, dashboard/CRM primary app, or generic office/content/code/video skill surfaces are rejected. | `contracts/web-gui-product-contract.json`, `frontend/web/index.html`, `tests/smoke/web-shell.test.mjs` |
| External authorities stay external | Desktop App packaging/updater, OPL Framework runtime truth, domain-agent judgment, billing truth, storage truth, node pool lifecycle, API gateway truth, OPL execution truth, and artifact/body authority are not owned by this repo. | `contracts/web-product-profile.json`, `contracts/web-runtime-bridge.json` |
| MedOPL owns the runtime business loop | Web runtime-required tasks call the Go control plane MedOPL bridge for `POST /api/opl/runtime-gate` and, only after MedOPL account/resource-state ready, `POST /api/opl/runs`. Web submits task intent and refs/context, then renders typed blockers, progress refs, deliverable refs, ledger refs, and deeplinks. MedOPL remains runtime/storage/package/billing/resource release owner; OPL Framework/domain agents remain execution semantics, quality verdict, and artifact authority. `MEDOPL_API_BASE_URL` is operator deployment config, not product admission policy. | `contracts/web-runtime-bridge.json`, `contracts/web-api.openapi.json`, `tests/contract/medopl-runtime-bridge-contract.test.mjs` |
| OPL runtime remains external authority | Real OPL CLI integration or Web-owned execution requires production MedOPL runtime execution evidence, eval, Web runtime command policy, and human authorization boundary. Web may bridge MedOPL gate/run refs-only projections, not mutate runtime or read private state. Selected/test/canary accounts can only be evidence fixtures or rollout safety, not product access policy. | `contracts/web-runtime-bridge.json`, Go control plane tests |
| Verification uses surface-level impacted lanes and release-time full gates | `verify` is the daily fast lane; `verify:dev` derives impacted lanes from the current diff. Ordinary UI surface edits validate their own `ui` lane, while app-shell, route/page-state, deploy/release, browser, integration, and full lanes are explicit and selected by changed surface or release risk. Registry metadata owns taxonomy and lane membership; lane-check/gate evidence decides whether targeted lanes were actually run for the current diff. | `AGENTS.md`, `scripts/test-classification.mjs`, `scripts/verify.mjs`, `scripts/lane-advisory.mjs`, `scripts/lane-check.mjs`, `scripts/workflow-gate.mjs`, `scripts/repo-bloat-audit.mjs` |
| Regression guards retire dynamically | Regression tests are temporary bug reproductions. `regression` may be empty; any `regression-guard` must declare retirement metadata, then delete its test and registry entry when the condition expires or is permanently covered by current-owner/integration tests. | `scripts/test-classification.mjs`, `tests/health/registry-coverage.test.mjs`, `docs/history/tombstones/README.md` |
| Maintain static HTML/CSS/ESM web shell until evidence says otherwise | Current Web UI stays as static HTML, CSS, and browser ES modules served by the Go control plane. React/Vite/TypeScript migration trigger is browser e2e becoming a release gate plus repeated product-state complexity, component reuse pressure, typed client need, or Docker/CI build evidence showing the static shell is now limiting delivery. | `frontend/web/`, `Dockerfile.cloud`, `tests/health/workflow-entrypoint.test.mjs` |
| Production dogfood is secret-gated HTTP evidence | Production authenticated dogfood proves the production account/session, API Key binding, optional ordinary chat, runtime gate, sanitized audit HTTP path, and optional readonly projection availability when `OPL_PRODUCTION_DOGFOOD_MEDOPL_READONLY=1`. It is not browser e2e, MedOPL runtime execution, payment, storage mutation, or node lifecycle evidence. | `contracts/web-release-profile.json`, `.github/workflows/cloud-rollout.yml`, `scripts/cloud-rollout.mjs`, `deploy/web-cloud/RUNBOOK.md` |
| Observability baseline stays no-secret until a real operations consumer exists | The current observability baseline is the no-secret public probe plus `/metricsz` schema used by release closeout and the scheduled production canary workflow. Do not introduce dashboards, alerting, error-budget machinery, or automatic rollback until there is an operational owner and consumer. | `contracts/web-release-profile.json`, `.github/workflows/production-canary.yml`, `backend/control-plane-go/cmd/opl-webui-control-plane/main.go`, `scripts/cloud-rollout.mjs` |
| Rollback is manual and evidence-gated, not automatic | Cloud Rollout supports a production-approved `rollback=true` path that runs `kubectl rollout undo`, rollout status, canary, smoke, and optional availability probe. The repo must not claim automatic rollback or migration rollback from this harness. | `.github/workflows/cloud-rollout.yml`, `scripts/cloud-rollout.mjs`, `deploy/web-cloud/RUNBOOK.md` |
| Commercial lifecycle stays a readonly personal projection | Current commercial lifecycle ownership is only authenticated readonly personal commercial status projection for the settings lifecycle summary. Team invite/RBAC/pricing/subscription/payment expansion needs a real consumer, contract, and registered tests while preserving MedOPL billing authority. | `contracts/web-product-profile.json`, `contracts/web-api.openapi.json`, `tests/contract/fixed-truth-lifecycle.test.mjs` |

## Superseded Readings

| Superseded reading | Current reading |
| --- | --- |
| Every formal change must create `changes/active/<change-id>/` with proposal, spec delta, design, tasks, eval plan, review, and closeout. | Fixed truth entries and registered tests are the source of current state. Historical closeouts are provenance only. |
| `docs/status.md` and `docs/decisions.md` were retired prose truth. | They are now required fixed truth entries, matching the One Person Lab App product-repo pattern. |
| `changes/archive/closeouts.md` is active evidence truth. | Historical closeouts live at `docs/history/process/closeouts.md`; active claims must point to current contracts, source, tests, scripts, and deploy artifacts. |
| A fixed durable file limit determines whether development can continue. | File count is report-only; owner boundaries, retired-surface guards, test registry, line hygiene, and fresh verification decide whether the repo is healthy. |
