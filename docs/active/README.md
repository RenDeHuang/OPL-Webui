# Active Gap Baton

- owner: product-engineering owner
- purpose: current gap, lane handoff, and next-agent context for parallel worktree development.
- state: active_support
- machine boundary: human-readable baton only; contracts, source, tests, scripts, API behavior, deploy artifacts, and Sentrux rules are authoritative.

## Role

`docs/active/` keeps the current Web product gap and worktree lane baton. It is not a process package, not a second status file, and not a log archive. When a lane closes, fold durable facts back into `docs/status.md`, `docs/decisions.md`, `contracts/`, source, tests, deploy artifacts, or `docs/history/`.

## Current Truth

One Person Lab Web product truth is fixed to three layers: Public Growth Layer, Account-based User Product Layer, and Minimal Admin/Ops Layer. Public Growth Layer is done v1 at repo/browser evidence level. Account-based User Product Layer is done v1 at repo/browser evidence level, including task history/deliverable continuation center v0. Minimal Admin/Ops Layer is partial: registration policy and user status v0 is done through operator-only API; dogfood/release evidence summary is not_started; hidden `/_ops` UI is optional/reserved. Production rollout latest-main evidence is folded back for commit `496261981494952a199d70e08cc0821d9e927eb3` through Cloud Rollout run `28323165665`. Historical run `28142197152` remains historical-only evidence and does not auto-apply to later main commits.

Active gaps are machine-owned by `contracts/web-product-profile.json#/gapMap` and `contracts/web-release-profile.json#/latestMainEvidence`: Admin/Ops dogfood/release evidence summary is not_started, hidden `/_ops` UI is optional/reserved, and future main commits need fresh latest-main foldback through `npm run release:evidence -- --run-id <github-run-id>`. `contracts/web-gap-phase-registry.json` keeps phase/eval policy; closed done phases stay as evidence history, not active product gaps.

Commercial product user journey depth is active under `contracts/web-gap-phase-registry.json#/gaps/commercial_product_user_journey_depth_v1`. Project/window continuation, window search, returning continuation, autonomy Inspector, ordinary `@科研` request-lifecycle first value, specialist MedOPL conversion handoff, Skill import, model selector, plus menu, and product acceptance browser E2E are repo/browser accepted. This does not prove token streaming, MedOPL readiness, a dedicated project/window persistence API, owner visual/copy acceptance, or production-ready SaaS.

1. Browser-level e2e runner executed successfully through `npm run verify:browser` with real Chromium/CDP for register, login, API Key binding, ordinary chat fallback, `@论文`/`@基金` gate, and sanitized audit evidence. It is now a CI release gate before image release; future non-CI hosts still need Chrome/Chromium via `OPL_BROWSER_BINARY` or system discovery.
2. Account-based Web App UX state is owned by the page-state contract, `RESEARCH_TASK_INTENTS`, `RESEARCH_RESULT_SECTIONS`, and `chatStateForPrompt`; the browser runner opens Web, logs in, binds an API Key, clicks the `research_direction` task template, verifies structured `@科研` result sections, verifies `@论文`/`@基金` runtime task cards, and keeps progress refs, deliverable refs, blocker/next step, and deeplink continuity as refs-only projection. The contract also exposes `@PPT` and `@书` as Foundry purpose entries that stop at the same MedOPL gate. This remains a browser UI view model and must not become artifact body, storage, or runtime execution.
2a. The current controlled launch readiness is separate from real upstream structured research success. `contracts/web-release-profile.json#/controlledLaunchReadiness` defines hard launch gates, supporting upstream evidence, service-unavailable fail-closed behavior, long-task refs/progress/deliverables/deeplink continuity, SaaS control-plane contracts, and cannot-claim boundaries. Latest main production evidence is folded back from run `28323165665` for commit `496261981494952a199d70e08cc0821d9e927eb3`, image `uswccr.ccs.tencentyun.com/webopl/opl-webui:4962619`; run `28142197152` remains historical only.
3. Production authenticated dogfood HTTP evidence passed in GitHub Actions run `28323165665` for commit `496261981494952a199d70e08cc0821d9e927eb3`, image `uswccr.ccs.tencentyun.com/webopl/opl-webui:4962619`, after dry-run, image preflight, production apply, Production Availability Probe After Apply, authenticated dogfood, and Production Browser E2E success. Production real ordinary chat, MedOPL readonly projection checks, and `chat.completed` audit evidence are folded back as sanitized latest-main release evidence. It remains secret-gated and not MedOPL runtime execution.
4. Production browser e2e evidence passed in GitHub Actions run `28323165665` through `production_browser_e2e=true` in `Cloud Rollout` with `apply=true`, `OPL_PRODUCTION_BROWSER_E2E=1`, `OPL_DOGFOOD_EMAIL`, `OPL_DOGFOOD_PASSWORD`, and `OPL_DOGFOOD_API_KEY`. The lane verified real Chromium login, API Key binding, task selection, structured result/gate, refs/deeplink continuity, and sanitized audit evidence against `https://opl.medopl.cn`. Real upstream structured research success is supporting evidence only; a later or future `service_unavailable` attempt must remain fail-closed evidence and cannot claim upstream provider availability.
5. Production browser e2e run `27877811961` failed only at the real Chromium lane after apply, availability probe, and authenticated dogfood passed. The audit evidence shows `chat.upstream_failed` with `upstreamKind=response_header_timeout`, `upstreamHost=gflabtoken.cn`, and `upstreamModel=gpt-5.5`; this is a production slow-header budget issue, not an API key/model configuration issue. Current remediation is the configurable `OPL_CHAT_UPSTREAM_TIMEOUT_SECONDS` budget and a wider production browser wait window.
6. Production dogfood readonly projection was confirmed for latest-main run `28323165665` with sanitized `OPL_PRODUCTION_DOGFOOD_MEDOPL_READONLY=1` foldback. It can claim production MedOPL readonly projection dogfood only; it is not runtime execution, payment readiness, storage mutation, node lifecycle, or production MedOPL API integration evidence. The later commercial cross-repo canary is recorded separately in `contracts/web-release-profile.json#/commercialCrossRepoE2ELaunchCompletion`: OPL-Webui Cloud Rollout run `28332168943` passed `Commercial Cross-Repo Browser Canary` for commit `d86c16354742e496d56327cbf7221cc6209fbf58`, paired with MedOPL Cloud Rollout run `28332365127` and redacted receipt hash `sha256:fa71a90c267503d617c4d196353b55c1f30a6e4e7eaaddbba6b60837a2442234`. The strict `Production Browser E2E` job in that WebUI run was skipped by design, so this is scoped consumer canary evidence only.
6a. Dynamic cross-repo current truth sync is recorded at `contracts/web-commercial-consumer-contract.json#/commercialCrossRepoDynamicCurrentTruthSync` and `contracts/web-release-profile.json#/commercialCrossRepoDynamicCurrentTruthSync`. It fresh-read MedOPL `recovery/platform-v22-trunk` HEAD `e6d2e7ae54ee3bcb2fd86fd21d56c35492ac7b35` and OPL-Webui `main` HEAD `94704dc55b9d689655d6e0a34625e8dd10d73b4a`, keeps owner truth split, and fails closed on ownership conflict, claim inconsistency, HEAD mismatch, missing contract field, or cannotClaim upgrade before live E2E, rollout, or production-complete claims. It is closed after sync and must be rerun only when a declared dynamic sync trigger changes.
7. Production availability probe succeeded in run `28323165665` through `Production Availability Probe After Apply`, sampling `/healthz`, `/readyz`, `/metricsz`, and `/` after the deployed image reached canary/smoke. It is not multi-node HA and not complete production-ready SaaS evidence.
8. Run `27878485498` was a release-order/image-missing issue, not product runtime failure: the workflow attempted image `uswccr.ccs.tencentyun.com/webopl/opl-webui:80689b1`, but the TCR manifest was absent. Current Cloud Rollout now inserts `production-image-preflight` after dry-run and before apply; `node scripts/cloud-rollout.mjs --image-preflight` fails closed as `image_missing_rollout_order_issue` with `imagePullOccurred=false` before `kubectl set image`.
9. Production observability baseline v1 is now wired into `/metricsz`, `scripts/cloud-rollout.mjs --availability-probe`, `contracts/web-release-profile.json`, and the no-secret scheduled workflow `.github/workflows/production-canary.yml`; latest-main release evidence is folded back to run `28323165665`, and scheduled canary first success is folded back to run `27874732529`. Operations readiness now has P0/P1/P2 contracts in `contracts/web-release-profile.json`: P0 launch operations, P1 commercial operations, and P2 SLA/HA operations gates are explicit. Alert severity owner receipt is accepted for owner `huangrende` with P0 5 minutes, P1 30 minutes, and P2 business-hours response; the concrete alert channel is still pending. Staging/load policy is accepted as staging 10 users baseline, production smoke only, and no real `gflabtoken.cn` quota consumption without separate authorization. `productionOperationsCloseout` is the P0/P1 single-node launch operations receipt contract and only accepts sanitized explicit evidence for rollback, alerting, DB restore, monitoring/dashboard, soak, load, DB pool, upstream backpressure, and migration compatibility through `npm run release:evidence -- --ops-closeout-json <sanitized-json>`. `productionLaunchCloseout` is the broader final release decision receipt contract. This is partial readiness only; rollback drill run id, DB restore drill, dashboard URL, and concrete alert channel remain pending external evidence. External evidence for P0/P1 closeout, final release receipt, production load, long soak, HA topology, SLO enforcement, and automatic rollback also remains pending.
10. Production HA is paused for the current single-node launch. The HA owner decision accepts no second TKE node, no `replicas=2`, and no HA claim in this phase. The apply manifest intentionally stays at `replicas=1` so a one-worker TKE cluster can keep rolling out safely. The release contract keeps the future HA evidence boundary: `replicas=2`, two Ready Pods, distinct nodes, `PDB minAvailable=1`, topology spread constraints, rolling update `maxUnavailable=0`, at least two healthy Ingress/CLB backends, canary/smoke, and production availability probe before any multi-node HA claim.
11. Manual rollback evidence is now wired into `Cloud Rollout` through `rollback=true` and `node scripts/cloud-rollout.mjs --rollback`. It is production environment approved and reuses canary/smoke gates; it is not automatic rollback and has no production rollback run yet.
12. Commercial account lifecycle is now an authenticated readonly personal commercial status projection: `GET /api/account/commercial-status` returns personal account, tenant role, active lifecycle state, `teamReadiness=single_user_owner`, allowed next action `view_medopl_billing`, and forbidden team/invite/RBAC/payment/billing-source mutation flags. Current browser UI keeps account/login/API Key behind the bottom avatar popover and keeps More as empty/light overflow; it must not dump lifecycle, quota, audit, provider, auth, or API Key status into More. Current work can only claim the authenticated readonly personal commercial status projection; team invite/RBAC/pricing/subscription/payment expansion requires a real consumer, contract, and registered tests while preserving MedOPL billing authority. It must not grow this into commercial admin, billing owner, workspace-visible UI, storage, node pool, or runtime ownership.
13. User-facing reliability status is now rendered in the browser shell for login/key/quota/runtime/upstream/service/network states. It is a sanitized UX view model only and must not include raw provider errors, API keys, DB URLs, private state, or artifact bodies.
14. Frontend engineering stays static HTML/CSS/ESM until browser evidence and product complexity justify React/Vite/TypeScript migration.
15. Retired Go task projection and OPL task-route surfaces must stay tombstoned; current DB canary validates the Web app schema.
16. UI/UX Product Depth now requires Figma MCP before further UI work. Current source evidence is pinned to Figma Make file `1MNO5l7PQYKZVNqQgw6DGS`, node `0:1`, with `src/app/App.tsx` and `src/styles/theme.css` as source files. The UI governance rule is subject-first: the subject is an AI-native research composer with project/session/result workflow, not a dashboard template, CRM template, settings center, runtime console, component inventory, or card grid first surface. Components follow surface ownership and behavior, not visual similarity: account/model metadata use popovers, project/window search and mobile inspector use sheets, API Key unblock uses a dialog, destructive confirmation alone uses alert dialog, and cards carry starter chips, library items, or result artifacts rather than routine section wrappers. The current repo-local UI variant is `ai_native_research_home_v1`: the shell uses side navigation, a first-view center composer, starter chips, route-scoped Skill/工作流/Projects surfaces, project/window search, empty More overflow, bottom-avatar account popover, optional model selector without visible provider base URL, one structured `@科研` artifact card instead of duplicated raw upstream transcript, a stable desktop right inspector work panel, and lightweight mobile bottom sheets. Rejected patterns are Drive/storage ownership, runtime truth ownership, founder-plan upsell, unlimited compute, dashboard/CRM primary app, generic office/content/code/video skills, default tech blue/purple primary palettes, visible fixed base_url in the user shell, settings/status dumps in More, fake refs/test evidence copy, floating desktop inspector overlays, heavy mobile sheets, raw structured-result transcripts, and retired shell structures. Repo-local responsive visual QA, OPL green, full interaction states, `prefers-reduced-motion`, keyboard path, API Key modal focus trap, contrast closeout, artifact-first result density, stable desktop inspector, mobile sheet height budget, visual identity, visual quality rubric, owner receipt protocol, and long-term accessibility/touch/performance/style/layout/typography/motion/forms/navigation quality gates are folded into `contracts/web-gui-product-contract.json`, `contracts/web-page-state-matrix.json`, and registered tests; they are no longer active phase contracts. The current UI/UX phase `production_ui_quality_claim` is accepted for the current production v1 surface through owner receipt and sanitized production browser evidence from run `28039468173`. Long-term truth is limited to `contracts/web-gui-product-contract.json`, `contracts/web-page-state-matrix.json`, `contracts/web-gap-phase-registry.json`, `contracts/web-product-profile.json`, registered tests, `docs/status.md`, and this baton; no new UI governance file is needed while those owners carry the rule. Temporary artifacts are `.runtime/browser-visual/*`, `.runtime/phase-runs/*`, production raw logs, raw screenshots, and CI raw output; they stay out of git and only sanitized summaries fold back. Complete UI/UX design system, assistive technology conformance, and production visual polish remain pending.
16a. Figma Make `UI UX for Commercial Launch` is now the deployed Figma parity UI replacement candidate in `contracts/web-gui-product-contract.json#/figmaParityUiReplacementTarget`, not a generic reference and not a partial visual reuse source. It applies to Public Growth Layer and Account-based User Product Layer only: product introduction, task scenarios, output showcase, start path, authenticated workbench, task entry, task history, refs, blocker/next step, and MedOPL/OPL deeplink. It is not an Admin/Ops, `/_ops`, or operator controls target. Production controlled launch run `28323165665` deployed commit `496261981494952a199d70e08cc0821d9e927eb3` / image `uswccr.ccs.tencentyun.com/webopl/opl-webui:4962619`; old UI surfaces are retired from the current production shell and must not be kept as a parallel shell. Current cannot-claim means current-stage evidence boundary, not permanent product vision denial: payment/commercial lifecycle, MedOPL-owned runtime execution, storage/artifact body, team/RBAC, and organization workspace can be future capabilities only after explicit consumer, contract, registered tests/eval, owner receipt, and folded evidence. Replacement copy follows canonical grammar: `Commercial Launch` becomes Controlled Knowledge Delivery / One Person Lab Web; `Pro`/`credit`/`recharge` become account capability, API key, quota projection, or external capability handoff; `runtime completed` becomes MedOPL continuation ready / refs available; `storage ready` becomes MedOPL storage status projection; `artifact body` becomes deliverable refs; and `payment status` becomes readonly commercial projection. Do not vendor the zip bundle, import its large dependency set, implement Admin/Ops UI, or expand release evidence beyond the controlled launch foldback.
17. Runtime/storage capability boundary is owner-accepted as local MedOPL gate/run refs-only projection without Web authority and now has repeatable real local MedOPL process evidence through `npm run verify:real-medopl`: runtime admission is driven by MedOPL account/resource state, not selected/test/canary users or hardcoded account allowlists. `MEDOPL_API_BASE_URL` is operator deployment config. runtime/storage/package/billing/resource release are MedOPL-owned capabilities when opened by MedOPL, but no OPL install, repair, module exec, mutation, artifact body, private state, storage mutation, billing mutation, or domain verdict is allowed from Web. The Go control plane now has a local MedOPL API bridge for `POST /api/opl/runtime-gate`, `POST /api/opl/runs`, and billing ledger refs projection. A real local MedOPL Go backend process verifies blocker, local credit/provider/open runtime+storage owner action, return_to_opl_task, run refs, artifact refs, billing ledger refs, and readonly release/storage projection through the WebUI bridge. Missing API key, missing package/plan/credit, compute, storage, workspace/runtime binding, billing blocked, release required, and audit required stay MedOPL account/resource typed blockers; missing endpoint is an operator deployment blocker. Production/sandbox MedOPL runtime execution/API evidence, external payment settlement, production release execution, Web-owned stop-billing, Web-owned storage destroy, tenant-scoped run audit closeout, and artifact/body authority remain unclaimed. `webRoutesMayMutateRuntime=false` is the current contract.
18. Commercial owner decision is accepted only for readonly personal projection: OPL-Webui keeps account/session, BYOK, and readonly personal commercial projection; billing/payment source of truth remains MedOPL; team/invite/RBAC/pricing/subscription/payment mutation remains blocked by structured expansion conditions for real consumer, surface contract, registered tests, MedOPL billing authority preservation, and payment processor contract. `webRoutesMayMutateBilling=false` is the current contract.
19. Operations maturity now means baseline plus P0/P1/P2 readiness contracts, P0/P1 single-node operations closeout contract, final release decision receipt contract, and the minimum cross-repo ops pointer at `contracts/web-release-profile.json#/commercialCrossRepoOpsReadinessMinimum`: dashboard URL, alert channel, error budget/SLO, DB restore drill, production load, long soak, production rollback execution, HA topology, final release approval, and automatic rollback remain unadmitted until sanitized external evidence is folded back. Current can claim release observability baseline, scheduled canary first success, rollback-record contract, P0/P1/P2 contract boundaries, alert severity owner boundary, staging/load authorization boundary, P0/P1 closeout contract, final receipt contract, and scoped commercial handoff runbook pointers only.
20. Current gap phases are explicit step gates rather than a broad wish list: UI/UX `production_ui_quality_claim` is accepted for the current production v1 surface; MedOPL readonly foldback is confirmed for latest-main run `28323165665` through sanitized release evidence; commercial runtime admission alignment is closed after the split Production Browser E2E passed in Cloud Rollout run `28323165665`; commercial depth is blocked on buyer/operator workflow plus expansion-condition evidence but no longer blocks single-node controlled launch; operations maturity is partial with P0/P1/P2 contracts present and external evidence pending; HA/resilience is paused on the single-node launch policy until multi-node evidence exists and no longer blocks single-node controlled launch; concurrency/load v1 is accepted as staging-safe 10-user baseline plus DB pool and slow-upstream fail-closed tests; OPL auto-update from GitHub is accepted as build-time pinned image-release policy with runtime sync forbidden.

21. Goal-driven UI work runs through `contracts/web-gap-phase-registry.json#/gaps/commercial_launch_ui_implementation`. `figma_to_code_implementation_map`, `figma_public_landing_slice`, `figma_auth_surface_slice`, `figma_home_workbench_shell_slice`, and `figma_dialog_sheet_projection_slice` are done with repo-local interaction/browser/current/diff-check evidence: Figma sections are mapped to OPL-Webui routes/components/tests, mock truth retirement is explicit, Public Landing has the Figma slice marker, canonical theme tokens, centered hero, seven task pills, primary/secondary start paths, trust strip, and pending task restore remains browser-guarded; Auth Surface has the Figma slice marker, centered card, two tabs, one visible submit, brand/subtitle, canonical placeholders, password toggle, and terms copy; Workbench Shell has the Figma slice marker, 240px side navigation, centered composer, toolbar, seven task launchers, project/window projection empty state, account trigger, and search trigger while preserving Go control-plane task data truth; Dialog/Sheet projection has Figma slice markers on search sheet, account popover, API key dialog, and inspector sheet, with open/close/Escape/focus-return, empty/error state evidence, and projection-only copy guards. The Commercial Launch UI implementation queue is closed at repo-local scope. Owner visual acceptance, production rollout, payment/runtime/storage/full SaaS capability, Admin/Ops UI, and release evidence changes remain unclaimed until separate evidence phases open.

22. Commercial Launch readiness closeout is closed after push, CI, release image, Cloud Rollout, split Production Browser E2E, authenticated dogfood, availability probe, and sanitized release evidence foldback for commit `496261981494952a199d70e08cc0821d9e927eb3`. This supports controlled launch readiness only; owner visual acceptance, production-ready SaaS, payment/runtime/storage/full SaaS capability, and Admin/Ops UI remain unclaimed. `commercial_product_maturity_gap_v1` is closed at repo-local maturity scope: deploy/config examples, backend verify, repo-local security scan, troubleshooting, backup/migration owner split, and planned contract-only minimal `/_ops` diagnostics are folded into existing contracts/docs/deploy/scripts/tests. Future setup wizard, standalone polish, MedOPL provisioning, payment, runtime/resource/storage, and generic installer work must open owner-specific queues instead of blocking ordinary UI development.

23. `commercial_product_user_journey_depth_v1` repo/browser product acceptance is done, with owner visual/copy receipt still pending. Visible IA is `项目 / 窗口`, search scope is `project_windows`, old task-history DOM/test anchors are retired from the current Web shell, ordinary `@科研` shows a request-lifecycle progressive first-value turn without claiming token streaming, specialist triggers show a MedOPL conversion handoff without claiming Web-owned runtime/storage/payment, and Inspector exposes autonomy/input/output/why-next context without artifact body authority.
24. `commercial_product_contract_stabilization_v1` fixes the durable product primitive baseline in `contracts/web-product-profile.json#/commercialProductPrimitives`: Project, Window, Turn, TaskIntent, Skill, ModelProfile, ComposerAction, InspectorSnapshot, and MedOPLHandoff. It is not production-ready SaaS or owner visual/copy acceptance.

## Worktree Lane Model

Use one worktree per independent slide or product gap. Each lane must declare:

- objective
- source of truth
- allowed write set
- forbidden write set
- verification commands
- stop condition
- foldback target

The root checkout stays on `main` for reading, review, final absorption, verification, commit, and push. Subagents may implement inside isolated worktrees only when their write sets do not overlap.

## Next Agent Context

Start from:

1. `README.md`
2. `AGENTS.md`
3. `TASTE.md`
4. `docs/project.md`
5. `docs/status.md`
6. `docs/decisions.md`
7. `docs/architecture.md`
8. `docs/invariants.md`
9. `docs/docs_portfolio_consolidation.md`
10. `contracts/*.json`

Then pick exactly one gap. Do not create `changes/active` packages. Do not add new recurring docs directories unless the new area has a stable owner, purpose, state, machine boundary, and active consumer.

For goal-driven work, check `contracts/web-gap-phase-registry.json` before editing. If the current phase is blocked by production evidence, owner receipt, or missing contract/eval, record `blocked` instead of claiming completion.

## Foldback Rules

- Product or API truth folds back to contracts and source.
- Current claim and cannot-claim changes fold back to `docs/status.md`.
- Durable decisions fold back to `docs/decisions.md`.
- Retired surfaces fold back to `docs/history/tombstones/README.md`.
- Verification evidence folds back to tests, scripts, deploy artifacts, and compressed process history only when the evidence changes the release or lifecycle claim.
- Production rollout completion triggers `scripts/release-evidence-sync.mjs` or equivalent compressed release evidence, then foldback to `contracts/web-release-profile.json`, `docs/status.md`, and this baton; active docs must not keep stale "not yet executed" claims after a GitHub run exists.
